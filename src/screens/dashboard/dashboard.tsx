import {Text} from 'react-native';

import {ScreenWrapper} from '@components';

export const Dashboard = () => {
  return (
    <ScreenWrapper>
      <Text>Dashboard</Text>
    </ScreenWrapper>
  );
};
